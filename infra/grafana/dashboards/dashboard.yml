apiVersion: 1
providers:
  - name: EMS AIoT Dashboards
    orgId: 1
    folder: EMS
    folderUid: ems-aiot
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards
